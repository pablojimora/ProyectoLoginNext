const toUpperCaseLetters = (data:string): string =>{
    return data.toUpperCase()
}

export const toLowerCaseLetters = (data:string): string =>{
    return data.toLowerCase()
}

export default toUpperCaseLetters